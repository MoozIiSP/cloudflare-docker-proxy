{
  "name": "cloudflare-docker-proxy",
  "private": true,
  "description": "A Docker registry proxy powered by Cloudflare Workers",
  "version": "1.0.0",
  "author": "ciiiii <allen.cccccnm@gmail.com>",
  "license": "MIT",
  "main": "src/index.js",
  "scripts": {
    "format": "bunx prettier --write '**/*.{js,css,json,md}'",
    "build": "bun run build-esbuild.js",
    "dev": "bun run build && wrangler dev dist/main.js --env dev",
    "deploy": "bun run build && wrangler deploy --env production"
  },
  "devDependencies": {
    "prettier": "^2.4.1",
    "wrangler": "^3.114.9"
  }
}